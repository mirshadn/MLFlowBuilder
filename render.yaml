services:
  - type: web
    name: no-code-ml-pipeline-builder-5
    runtime: python3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT

